-Authentication JWT 
1.Frontend : username , password  =>POST REQUEST
2.backend : verifies password stored in database using hashing
compare enterend pass vs actual pass using BCRYPT 
3.if matches jwt generates a token 
4.then that token is stored in localstorage
5.Include token in headers 
6.Server verifies the token 
7.User permission is checked out user.role using middleware 




-Payment using Razorpay 
1.get key_id , key_secret from Razorpay dashboard
2.install razorpay SDK 
3.



-Role based access 
the middleware check for instructor or student








//Node JS / ExpressjS
npm install express mongoose dotenv cors











-REST Api 
-middleware 
-bcryot 
-nodemailer



const Login = async(req , res )=>{
    try{
        const {email , password} = req.body; 

        if(!email || !password){
            return.res.status(404).json({
                success:false,
                message: "All fields are required . "
            })
        }

        const existingUser = await User.findOne

    }catch(error){

    }
}